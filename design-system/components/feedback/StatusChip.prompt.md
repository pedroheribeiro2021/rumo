Compact status pill — used on the price monitor ("no alvo" / "perto" / "acima") and could tag expense/category state elsewhere.

```jsx
<StatusChip tone="good">no alvo</StatusChip>
<StatusChip tone="warn">perto</StatusChip>
<StatusChip tone="bad">acima</StatusChip>
```
